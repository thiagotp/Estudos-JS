function exponencial(base, expoente) {
    console.log(Math.pow(base, expoente))
}

exponencial(2, 4)

let exponencial2 = (base, expoente) => {
    console.log(Math.pow(base, expoente))
}

exponencial2(5, 3)